name: test

on:
  discussion:
    types:
      - "created"
      - "edited"
  discussion_comment:

env:
  T: "tTdly-Old"
  F: "ttdly"

jobs:
  testt:
    runs-on: ubuntu-latest
    steps:
      - run: echo ${{toJSON(github)}}